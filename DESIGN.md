# RockAndScissPaper — 기획서

가위바위보를 소재로 한 TCG(트레이딩 카드 게임). 1:1 대전.
아직 초안 단계 — 아래 "확인 필요" 항목들은 추후 확정 예정.

**2026-08 기준 진행 중인 개편**: 1인칭 팔만 보이던 연출을 3인칭으로 교체하면서
승패 판정 자체도 함께 바뀌었다 — 아래 "체력과 승리 효과" 참고. 이 개편에 따라
능력카드(리셋/교체/변화/드로우) 넷은 카드 시스템에서 완전히 빠지고, 그 자리를
아래 "아이템" 섹션의 새 목록이 대신한다 — 4종이 그대로 옮겨가는 것이 아니라
**폐기될 수도 있는 별개의 설계**다. 이 문서에서 능력카드 관련 서술은 그 이전을
기준으로 한 참고 자료로 남겨두었다.

## 개요

- 장르: 가위바위보 상성 기반 TCG
- 대전 방식: 1:1, **체력제** — 양쪽 10체력에서 시작해 먼저 0이 되는 쪽이 매치 패배
- 시점: 3인칭. 테이블을 사이에 두고 마주 앉은 내 캐릭터와 상대가 함께 보이고,
  카메라는 내 캐릭터 머리 중앙 위치에서 내 손·테이블·상대가 보이게 잡는다 —
  이게 기본 화면
- 카메라 상호작용 (호버+클릭 줌인안은 폐기, 아래로 대체):
  라운드에 진입하면 마우스로 카메라를 자유롭게 회전할 수 있다 — 기본 정면 축
  기준 상하좌우 약 60도 범위. 스페이스바를 누르면 카메라가 손패 쪽으로 블렌드
  전환되어 내 패를 확인할 수 있다. 그 상태에서 카드를 골라 제출하면 카메라가
  다시 기본 시야로 블렌드 복귀하면서 제출 애니메이션이 재생되고, 낸 카드가
  제출칸에 들어간다. 아이템 사용 시 카메라 동작은 아직 미정(아이템 자체가
  설계 전)

## 아트 방향

**유치한 소재를 음침하게 다룬다.** 가위바위보라는 소재와 화면의 온도차 자체가 컨셉이다.

이 방향은 새로 정한 것이 아니라 이미 그려진 카드(`Assets/Cards/`)에서 읽어낸 것을
말로 옮긴 것이다. 앞으로 그릴 카드가 기준으로 삼을 문장이 없어서 적어둔다.

### 정해진 것

- **완전 모노크롬.** 회색 금속·석판. 채도 없음
- **강조색은 딱 한 곳 — 조커.** 게임에서 유일하게 색을 가진 것이 최우선 소멸카드다.
  색이 그 한 장에만 있으면 조커가 등장할 때마다 화면이 반응한다.
  **다른 어디에도 색을 쓰지 않는 것이 이 규칙의 전부다** — 하나 늘어나는 순간 효력이 사라진다
- **손은 맨손이 아니라 판금 건틀릿.** 카드에 그려진 주체가 사람이 아니다
- **오컬트 원반** — 손 뒤의 십자선 도형. 뒷면 전체를 덮는 것과 같은 계열
- **각진 모서리 장식과 베벨 프레임**, 음각으로 새긴 질감

### 원반은 등급을 표시한다

원반 무늬를 일반 / 능력 / 조커 / 공백 별로 다르게 그린다.

그림을 못 알아봐도 **무늬만으로 카드 종류가 먼저 읽히게** 하기 위한 것이다.
어둡거나 비스듬히 보이는 화면에서 중앙 그림은 뭉개져도 원반의 실루엣은 남는다.

### 명판에는 글자를 굽지 않는다

하단 명판은 **비워둔 채로** 그린다. 그래야 한국어/영어 전환이 그림을 갈아끼우지 않고
동작한다 ([CLAUDE.md](CLAUDE.md)의 번역 규칙).

> ⚠️ **덧씌우는 쪽이 지금 없다.** 2D `CardView`가 `_nameLabel`로 덮던 것인데, 3D
> `CardView`에는 라벨이 없다 — 카드에 이름을 그리는 코드가 어디에도 없고, `DisplayName`을
> 읽는 곳은 경기 로그 패널과 2D 디버그 하네스뿐이다. 즉 **지금 이대로 명판을 비워 그리면
> 이름이 아무 데도 안 나온다.** 6종을 그리기 전에 3D에서 이름을 어떻게 얹을지부터
> 정해야 한다 (`Label3D`, 머티리얼 오버레이, 아니면 호버 시 별도 패널).

### 동사를 그리는 법 — 건틀릿을 상수로 둔다

가위·바위·보는 사물이라 그리면 되지만 나머지 여섯은 **동사**다.
프레임 구성(`프레임 + 원반 + 건틀릿 + 명판`)을 고정하고, **건틀릿이 무엇을 하는지만** 바꾼다.

| 카드 | 건틀릿 |
|---|---|
| 변화 | 바위와 가위 사이에서 멈춘 손 — 기존 세 포즈의 중간 |
| 공백 | 펼친 빈 손, 또는 원반만 있고 손이 없음 |
| 조커 | 무언가를 부수는 주먹. **유일하게 색이 들어가는 카드** |
| 리셋 | 두 손이 비워짐 — 유일하게 상대에게도 영향을 주므로 혼자 달라 보여야 한다 |
| 교체 | 두 손이 엇갈려 지나감 |
| 드로우 | 더미에서 뽑아 올림 |

### 해상도

뒷면(`CardBackImage.png`, 1059 × 1485)을 기준으로 한다.

현재 앞면 세 장은 `CardSprite.png` 안에서 장당 약 90 × 128 — **뒷면보다 선형 12배 작다.**
2D 썸네일로는 버텼지만 카드를 크게 보여주는 순간 뭉갠다.
**세 장도 다시 그려야 한다.**

### 남은 것

- 강조색이 정확히 무슨 색인지 미정 (붉은색 계열이 기본 후보)
- 아직 아트가 없는 카드: 공백, 드로우, 조커, 리셋, 교체, 변화 **여섯 종**

## 상성

- 가위 → 보 (가위가 보를 이김)
- 바위 → 가위 (바위가 가위를 이김)
- 보 → 바위 (보가 바위를 이김)
- 같은 카드끼리 내면 **무승부**

## 체력과 승리 효과

**10선승제를 대체.** 양쪽 10체력에서 시작하고, 일반카드(가위바위보)끼리 붙어 승패가
갈린 라운드마다 이긴 카드에 따라 정해진 만큼 진 쪽 체력이 깎인다. 무승부이거나
능력/공백/조커가 낀 라운드는 이전과 마찬가지로 체력 변동이 없다.

| 이긴 카드 | 데미지 | 연출 | 부가 효과 |
|---|---|---|---|
| 바위 | 2 | 상대 얼굴을 주먹으로 침 | 없음 |
| 가위 | 1 | 상대 왼손(내 오른손 앞)을 가위로 찔러 고정 | 상대는 다음 턴 아이템 사용 불가 |
| 보 | 1 | 오른손으로 상대 앞 책상을 내려침 | 상대 **현재** 패가 약 3초간 공개됨 (다음 드로우로 뭘 뽑을지는 여전히 모름) |

- 가위 승의 "아이템 사용 불가"는 아이템 자체(아래 "아이템" 섹션)가 구현되어야 걸 곳이
  생긴다. 보 승의 "패 공개"는 손패를 3D 씬에 놓는 작업이 먼저 필요 — 둘 다 지금은 규칙만
  정해둔 상태
- 매치 승리 조건은 상대 체력을 먼저 0으로 만드는 것. **이제 이것이 유일한 종료 조건이다** —
  덱 고갈 패배는 없어졌다(아래 「덱 — 없다」 참고)

## 덱 구성 (플레이어당)

**개편 대상 — 지금은 미확정. 다만 구현은 이미 더 내려가 있다.**

- 이전: 총 **17장** = 가위/바위/보 각 3장(9장) + 공백카드 2장 + 조커카드 2장 + 능력카드 4장
- 능력카드(리셋/교체/변화/드로우) 4장은 카드 시스템에서 빠져 아이템으로 옮겨갈 예정
- **현재 구현은 9장이다** — 가위/바위/보 각 3장뿐. 능력카드에 더해 **공백과 조커도
  덱에서 뺐다.** 셋의 공통점이 "그 라운드에 승패가 없다"는 것이라, 남겨두면 승패 없는
  라운드가 예외가 아니라 일상이 되고 체력·연출이 대부분의 라운드에서 아무것도 안 한다.
  매 라운드가 실제 판정과 데미지와 연출을 내도록 일단 전부 뺀 상태다
  ([Scripts/Cards/DeckAssembler.cs](Scripts/Cards/DeckAssembler.cs))
- 뺀 것은 **지우지 않았다.** `ECardName`에 이름이 남아 있고 `RoundResolver`가 여전히
  해결하며 테스트도 그대로 통과한다 — 되돌리는 것은 `DeckAssembler`에 `AddCopies` 한 줄
- 최종 구성은 아이템 시스템이 확정된 뒤에 정한다. 공백·조커를 되돌릴지도 그때 같이
- 공백카드는 **승패 판정에는 참여하지 않음** — 낸 후 소멸하는 기존 규칙은 그대로 유지

## 손패와 보충

- **5장**을 받고, 라운드마다 1장씩 낸다. 구성은 정해져 있지 않다 — 완전 랜덤이다
- **5장을 다 쓰면 그때 5장을 새로 받는다.** 라운드마다 한 장씩 채워지지 않는다
- 그래서 서로 상대에게 몇 번의 수가 남았는지는 항상 알지만, 그게 무엇인지는 모른다.
  이 리듬이 이 규칙의 전부다

## 카드 분류

- **일반카드**: 가위/바위/보 카드 (덱당 각 3장, 총 9장) — **현재 덱에 들어가는 유일한 종류**
- **공백카드**: 효과 없이 소멸하는 카드 (덱당 2장) — 현재 덱에서 빠져 있음, 위 「덱 구성」 참고
- ~~**능력카드**: 별도 효과를 가진 카드~~ — 아이템 시스템으로 이전 예정. 아래
  "능력카드(4종류, 현재 전부 확정)" 섹션은 옮기기 전 참고용으로 남겨둔 것
- **조커카드**: 덱당 2장, 상대 카드를 소멸시키는 최우선 처리 카드 (아래 별도 섹션 참고). 조커는 카드 자체이므로 이번 개편에서도 그대로 카드로 남는다 — 다만 **현재 덱에서는 같이 빠져 있다**, 위 「덱 구성」 참고

## 라운드 진행

매 라운드, 양 플레이어가 카드 1장씩 동시 제출. **제출된 카드는 반드시 "덱 바닥으로 복귀" 또는 "소멸" 둘 중 하나로 처리되고, 그 후 양쪽 모두 예외 없이 새 카드 1장씩 드로우함** — 패로 돌아가거나 드로우가 생략되는 경우는 없음.

승패 판정은 **오직 일반카드(가위바위보) 간의 상성 비교로만** 이루어짐. 능력/공백/조커가 낀 라운드는 승패 자체가 존재하지 않으며, 체력에도 영향을 주지 않음.

카드 종류별 기본 처리:
- **일반카드**: 승/패/무승부 결과와 무관하게 항상 **덱 바닥으로 복귀**
- **능력카드**: 효과 발동 후 **소멸**
- **공백카드**: 효과 없이 **소멸**
- **조커카드**: 상대 카드를 소멸시키고(아래 참고) 자신도 함께 **소멸**

**양쪽 다 능력계열(능력/공백/조커) 카드를 낸 경우** — 아래 우선순위로 처리 순서를 정함:
1. **조커** — 최우선
   - 조커 vs 조커: 아무 효과 없이 서로 소멸
   - 조커 vs 그 외 카드: 상대 카드를 소멸시키고 그 효과를 차단(발동 자체를 막음). 조커 자신도 함께 소멸
2. **리셋** — 조커가 없을 때 그다음 우선순위. 능력카드끼리 겹쳤을 때 리셋이 있으면 리셋부터 처리. 양쪽 다 리셋이면 순서대로 2번 실행
3. **그 외 능력카드** (교체/변화/드로우)
4. **나머지** (일반카드, 공백카드)

동순위끼리 겹치면 **Player 1의 카드부터** 처리. 실행큐는 Player 1 → Player 2 순으로 채운 뒤, 위 우선순위(조커 > 리셋 > 능력 > 나머지) 기준으로 재정렬해서 처리한다.

제출된 카드는 가운데(중앙 필드)에 기록으로 남아 양측에 공개됨 — 지금까지의 매치 기록 열람 가능

## 덱 — 없다

플레이어가 보는 덱은 없다. 장수도, 고갈도 없다. 카드는 다섯 장씩 도착할 뿐이고, 더 낼 카드가
없어서 지는 라운드는 존재하지 않는다.

**이전 규칙(덱이 비면 즉시 매치 패배)은 폐기됐다.** 구현상으로는 `Deck`이 남아 있지만 비면
스스로 같은 구성으로 다시 채워지므로 바닥나지 않는다 — 즉 규칙이 아니라 보급원이다.
`MatchSession.Winner`도 이제 체력만 본다.

고정 길이(예: 50장)를 미리 만들어두는 방법도 있었지만 택하지 않았다. 길이가 정해진 목록은
결국 끝이 있고, 그러면 "끝에 도달하면 어떻게 되나"를 규칙으로 정해야 하는데, 그 규칙은 설계상
일어나지 않아야 할 상황만을 위해 존재하게 된다.

## 능력카드 (4종류, 현재 전부 확정) — 폐기 후보, 참고용

**카드도 아이템도 아니게 될 가능성이 있다.** 아래 서술은 능력카드가 카드/덱의 일부였던 이전 설계를 그대로 남겨둔 것이다 — 새 아이템 목록(아래 "아이템" 섹션)은 이 4종을 옮긴 것이 아니라 별개로 설계됐고, 이 4종이 그대로 다시 쓰일지는 정해지지 않았다.
지금은 아래 4종이 전부이며, 덱에 전부 포함됨(선택 없음). 사용 시 효과 발동 후 무조건 소멸. 가칭 이름은 편의상 붙인 것, 확정 아님:
1. **리셋** — 나와 상대 둘 다 패를 덱에 넣고 셔플한 뒤, 각자 원래 패 수만큼 다시 드로우 (유일하게 상대에게도 영향을 주는 카드)
2. **교체** — 패에서 **최대 2장**까지 덱에 넣고 셔플한 뒤, 그만큼 드로우 (넣을 카드는 공개 후 선택). 0장도 가능하며, 그 결과는 시간 초과와 같다
3. **변화** — 패에 있는 일반카드 또는 공백카드 1장을 원하는 카드(일반카드 또는 공백카드)로 변신 (대상과 결과는 공개 후 선택)
4. **드로우** — 카드 2장 뽑기

**보충**(덱에 공백카드 2장을 추가로 넣고 셔플)은 규칙에서 빠졌다. 코드는 지우지 않고 [Deprecated/](Deprecated/README.md)에 두었다.

### 선택 시점 — 카드 공개 후

**리셋 / 드로우**는 선택이 필요 없다. **교체 / 변화**만 선택이 필요하고, 그 선택은 **양쪽 카드가 공개된 뒤에** 한다.

라운드는 2단계로 진행된다:

```
제출 → 공개 → (선택) → 효과 적용 → 결과 발표
```

- 선택은 **공개된 상대 카드를 보고** 하게 된다
- 양쪽 다 선택이 필요한 카드를 냈다면 **동시에 각자** 고른다. 서로의 선택 내용도, 그 결과도 보지 못한다 (Player 1 우선 규칙은 선택 단계에는 적용되지 않음)
- 고르는 대상이 되는 패는 **내가 낸 카드가 이미 빠져나가고, 리셋까지 처리된 뒤의 패**다. 리셋/드로우는 선택을 묻기 전에 먼저 실행된다 — 우선순위표가 원래 의미하던 순서 그대로다
- **조커에 막힌 쪽은 선택 자체를 요구받지 않는다.** 골랐다가 버려지는 것이 아니라 애초에 묻지 않음
- **고를 수 있는 것이 하나도 없는 쪽도 요구받지 않는다.** 같은 이유다 — 어떻게 답해도 거부될 선택을 요구하지 않는다. 변화를 냈는데 리셋이 먼저 처리되어 새로 받은 패에 일반카드/공백카드가 한 장도 없는 경우가 실제로 여기 해당한다(초반 한정, 확률 약 0.05%). 효과는 그냥 발동하지 않으며, 아래 시간 초과와 결과가 같다

**제출 시간 제한 — 20초**: 시간 내에 카드를 내지 않으면 **호스트가 그 플레이어의 패에서 무작위로 1장을 대신 낸다.** 벌점이 아니며 라운드는 평소대로 진행된다. 무작위인 이유는 두 가지다 — 패 순서에는 아무 의미가 없으므로("첫 번째 카드"는 삽입 순서의 우연일 뿐) 어느 한 장을 고를 근거가 없고, 소멸하는 조커/능력를 지켜주는 규칙은 답하지 않는 쪽에 보상이 되기 때문이다.

**선택 시간 제한 — 15초**: 제한 시간 내에 선택하지 않으면 그 효과는 발동하지 않는다. 교체의 경우 "0장 교체"와 결과가 같다. 카드는 어느 쪽이든 소멸한다. 제한 시간은 양쪽 선택을 통틀어 한 번 돌아가며, 화면에는 게이지로 표시된다.

**선택의 공개 범위**: 상대에게는 **몇 장을 바꿨는지(교체)** 와 **변화가 발동했는지** 만 공개된다. **어떤 카드를 덱에 넣었는지 / 무엇을 무엇으로 바꿨는지는 공개되지 않는다.**

## 아이템

**메쉬로 테이블(item slot)에 놓는다 — 결정됨.** 스트림덱 버튼형 UI 안은 폐기. 능력카드처럼
카드로 내는 것이 아니라, 라운드와 별개로 아이템 슬롯의 오브젝트를 직접 사용하는 액션이 될
예정이다. 정확한 사용 시점(제출 전/후, 라운드당 제한 등)은 아직 미정.

우선 아래 3종으로 시작하고, **종류는 이후 계속 추가될 수 있다**:

1. **회복** — 체력 +2. **오버힐 가능** — 10을 넘어설 수 있다 (체력 상한 자체가 없어지는지, 표시만 10을 넘게 되는지는 미정)
2. **배가** — 이번 라운드에 내가 주는 피해 2배
3. **리셋 (나만)** — 내 패만 덱에 넣고 셔플한 뒤 원래 패 수만큼 다시 드로우. 기존 능력카드
   "리셋"과 달리 상대에게는 영향을 주지 않는다

세부 규칙(코스트, 재사용 조건, 라운드 중 사용 가능 시점, 상대에게 공개되는 정보의 범위 등)은
전부 미정. 위 "능력카드" 섹션의 선택/타이밍 규칙을 그대로 가져다 쓸 수 있는지도 아직 검토 전.

## 공백카드

- 덱당 2장, 효과 없음. 카드를 낸 후 그대로 소멸
- 내 카드를 소모하지 않으면서 상대가 이번 라운드에 뭘 냈는지 확인하는 "패 까기"용 카드
- 공백카드끼리 만나면(양쪽 다 공백인 경우) 조커와 마찬가지로 서로를 겨냥하지 않고 두 장 다 그대로 소멸
- 이 "정찰" 효과는 사실 공백카드만의 것이 아니라, 능력카드가 낀 라운드 전체에 공통으로 발생하는 부수 효과임 (승패 판정 없이 카드가 중앙 필드에 공개되기 때문). 공백카드는 그중 다른 부가 효과 없이 이 정찰 역할만 하는 카드

## 조커카드

- 덱당 2장, **최우선 소멸카드** — 다른 카드 효과보다 먼저 처리됨
- 상대가 그 라운드에 제출한 카드 1장을 소멸 (일반카드/능력카드 무관). 상대 카드가 능력카드였다면 그 효과 자체도 발동되지 않고 차단됨 — 교체/변화였다면 **선택을 요구받지도 않는다**
- 조커 자신도 사용 후 함께 소멸 (덱 복귀 없음)
- 양쪽 다 조커를 내면 서로를 소멸시키는 대신 조커 두 장이 그대로 소멸
- 우선순위/충돌 처리는 [라운드 진행](#라운드-진행) 참고

## 확장성 메모

- 드로우는 카드 종류 구분 없이 셔플된 덱 전체에서 이루어짐 (능력카드도 당연히 드로우 대상)
- 능력카드는 지금은 4종이 전부이고 전부 덱에 고정 포함되지만, **나중에 풀이 늘어나면 그중 일부를 고르는 덱빌딩 선택 기능으로 확장될 수 있음**. 구현 시 이 확장을 막는 하드코딩(예: "능력카드는 항상 이 4종"이라고 못박는 로직)은 피할 것 — 관련해서 [CLAUDE.md](CLAUDE.md)에도 반영
- 아직 확정되지 않은 확장 아이디어(덱 장수 커스텀, 클라이언트별 덱 저장/매칭, 덱 고갈 규칙 등)는 [IDEAS.md](IDEAS.md)에 낙서로 모아둠. 확정되면 이 문서로 옮길 것
