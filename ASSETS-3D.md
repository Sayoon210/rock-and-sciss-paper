# 3D 자산 목록 (준비 단계)

`rebuild-3d` 브랜치 기준. [IDEAS.md](IDEAS.md) §6의 견적과 짝을 이룬다.

> ## ⚠️ 아래 「확정된 방향」은 2026-08-28에 폐기됐다
>
> 1인칭·팔만 보이는 구성이 **3인칭·얼굴과 상반신이 보이는 구성으로 뒤집혔다.** 승패도
> 10선승에서 체력제로 바뀌고, 승리 수단(바위/가위/보)마다 다른 연출과 피해가 붙는다.
> 경위는 [DevLogDoc/2026-08-28-presentation-layer-swap.md](DevLogDoc/2026-08-28-presentation-layer-swap.md).
>
> **아직 유효한 절**: 「조명」, 「안개」, 「임포트 설정」, 「치수」, 「카드」 — 시점과 무관하다.
> **무효인 절**: 「확정된 방향」, 「그림자 컷이 오히려 싸다」, 「남은 갈림길 하나」,
> 「자산 목록」의 팔 관련 전부(판금 건틀릿/기계팔 갈림길 포함). 팔만 만들면 된다는 전제가
> 사라졌으므로 캐릭터 전신이 필요하고, 실제로 Mixamo 캐릭터를 베이스로 쓰고 있다.
>
> 새 방향이 굳으면 이 문서를 다시 쓴다. 그 전까지 지우지 않는 이유는 조명·안개 레시피가
> 아직 이 문서에만 있어서다.

## 확정된 방향 — 벅샷 룰렛식 1인칭 (폐기됨, 위 참고)

- **탁상에 앉은 1인칭 시점.** 카메라가 테이블 위를 내려다본다
- **양쪽 다 팔만 등장한다.** 손목 위쪽은 어둠에 잠겨 보이지 않는다
- 벅샷 룰렛과 다른 점: **얼굴이 없다.** 상대도 팔뿐이다

이 한 줄이 아래 전부를 결정한다. 카메라 각도, 조명, 톤, 그리고 **카드 아트까지.**

---

## 이 결정이 푼 것 두 개

### 1. 카메라 각도가 정해졌다

이전 버전 문서에서 "각도가 안 정해져서 아무것도 발주하면 안 된다"고 적었는데,
**1인칭 착석 시점으로 확정되면서 그 블로커가 사라졌다.** 이제 무엇이 화면에 보이는지 안다:

- 탁상 상판 — **크게 보인다.** 화면의 대부분
- 탁상 다리/측면 — **안 보인다.** 만들 필요 없음
- 카드 앞면 — 비스듬히 위에서. 두께는 살짝
- 방, 벽, 천장 — **검다. 자산 없음**

### 2. 카드 아트의 톤이 정해졌다

**이게 더 중요하다.** 아트 없는 카드가 이 프로젝트의 실질 블로커인데, 그릴 근거가 없어서
시작을 못 하고 있었다.

확정된 방향은 [DESIGN.md](DESIGN.md)의 「아트 방향」절에 있다 — 모노크롬 판금 건틀릿,
오컬트 원반, 조커에만 쓰는 강조색 하나. 사실 새로 정한 게 아니라 이미 그려진 세 장에
있던 것을 읽어낸 것이다.

**여기(3D)에 영향을 주는 것만 다시 적으면:**

- **조커가 화면에서 유일하게 색을 가진 것이 된다.** 검은 배경에 스포트라이트 하나뿐인
  씬이라 이 대비가 2D에서보다 훨씬 세게 먹는다. 조커 소멸 연출에 그 색을 쓴다
- **원반이 등급을 표시한다.** 어둡고 비스듬한 화면에서 중앙 그림은 뭉개져도
  원반 실루엣은 남는다 — 이 결정이 3D 가독성 문제를 절반 정도 해결한다
- **건틀릿이 씬의 팔과 같은 물건이다** (아래 참고)

---

## 그림자 컷이 오히려 싸다

팔을 넣는 게 비싸질 거라고 봤는데, **어둠에서 잘리는 조건이 붙으면 반대다.**

| 안 만들어도 되는 것 | 이유 |
|---|---|
| 어깨, 몸통, 머리, 얼굴 | 프레임 밖 |
| 전신 리그 | 손목 위로 뼈가 필요 없음 |
| 방, 벽, 가구 | 검다 |
| HDRI 환경맵 | 주변광이 거의 없어서 반사할 환경이 없음 — 미리 받아둘 필요 없이,
  스파이크에서 금속(팔)이 완전히 죽어 보이면 그때 낮은 세기로 테스트 |
| 탁상 다리·측면 | 시야 밖 + 어둠 |

**메쉬는 어둠 속에서 그냥 끝나면 된다.** 잘린 단면은 보이지 않는다.
다만 단면은 막아둘 것 — 빛이 조금이라도 새면 뚫린 팔이 보인다.

### 소맷단을 붙이면 두 개가 동시에 해결된다

잘리는 지점에 **소매/커프스**를 두면:
1. 단면이 원천적으로 안 보인다 (조명이 새도 안전)
2. **내 팔과 상대 팔을 구분하는 자리**가 된다 — 색이나 재질만 바꾸면 되고,
   토폴로지를 새로 만들 필요가 없다

### 손은 하나만 만들면 된다

카드를 집어서 내려놓는 동작은 **한 손이면 충분하다.** 그 메쉬를 미러링해서 반대 손,
상대 팔까지 재활용한다. 상대는 재질/소매만 다르게 간다.

---

## 조명 — 이제 여기가 자산의 핵심이다

메쉬가 아니라 **조명이 이 게임의 아트가 됐다.** 팔이 어디서 잘리는지를 조명이 정한다.

- **`SpotLight3D`** 하나를 테이블 위에서 아래로. `DirectionalLight3D`는 감쇠가 없어서 이 연출에 못 쓴다
- **빛 웅덩이가 테이블보다 좁아야 한다.** 내 팔은 화면 앞쪽에서, 상대 팔은 건너편에서
  들어오는데 **양쪽 다 어둠에서 나와야 한다.** 빛이 테이블 양 끝에 닿으면 두 팔의
  잘린 지점이 다 드러난다 → 스포트 각도와 테이블 크기가 서로를 구속한다
- **그림자 켤 것** (`ShadowEnabled`). 카드가 탁상에 드리우는 그림자가 3D의 절반이다
- **`Environment.VolumetricFogEnabled`** + `Light3D.LightVolumetricFogEnergy` —
  빛줄기가 보이는 그 느낌. 벅샷 룰렛 분위기의 큰 지분이고 **자산이 0이다.** 설정값뿐

---

## 남은 갈림길 하나 — 내 패를 어디에 두나

**팔의 비용이 여기서 갈린다.** 아직 안 정해졌고, 이게 다음 결정이다.

### A. 탁상에 눕힌다 (권장)

내 카드 4~6장이 내 앞 탁상에 고정 슬롯으로 깔리고, 손이 **집으러 간다.**

- **벅샷 룰렛이 실제로 하는 방식이다.** 아이템이 테이블에 놓여 있고 손을 뻗는다
- 슬롯 위치가 **작업 시점에 고정**이라 → 애니메이션을 **Blender에서 구워오면 끝**.
  런타임 IK 불필요 (`SkeletonIK3D`는 있지만 안 써도 된다)
- 패 장수가 변해도 상관없다 — 빈 슬롯은 그냥 빈 슬롯
- 손이 카드를 가리지 않는다
- **약점: 가독성.** 눕힌 카드는 비스듬히 보여서 찌그러진다.
  → 벅샷 룰렛이 쓰는 해법 그대로, **집을 때 카메라가 잠깐 기울거나 카드가 들린다**

### B. 손에 부채꼴로 든다

- 가독성은 최고
- **대가가 크다**: 장수마다 포즈가 달라야 하고(4장/6장/교체 중), 손이 카드를 가리고,
  구운 애니메이션으로 안 되니 IK가 필요해진다

→ **A를 권한다.** 레퍼런스와도 맞고, 팔 애니메이션이 "굽는 것"으로 끝난다.

---

## 자산 목록

### 팔 (직접 제작) — 카드 속 건틀릿과 같은 물건

**맨손이 아니라 판금 건틀릿이다.** 카드에 그려진 손과 씬에서 카드를 내는 손이 같아진다 —
카드가 그 카드를 내는 손을 그린 그림이 된다.

**스켈레톤이 없다.** 카드를 쥐는 순간 손가락이 열렸다 닫히는 것을 보여주지 않기로 했으므로
(집는 손가락 애니메이션 자체가 불필요), 건틀릿을 **이미 카드를 쥔 모양으로** 한 덩어리로
모델링하고 오브젝트 단위 Location/Rotation 키프레임만 준다. 스키닝도, 리깅도 없다 —
집는 느낌은 전부 모델링된 손 모양이 전달한다.

| 항목 | 사양 |
|---|---|
| 메쉬 | 건틀릿 + 전완 판금. 이미 카드를 쥔 자세로 조각. 팔꿈치 근처에서 절단, 단면 막음 |
| 소맷단 | 절단부를 덮는 커프스. **내/상대 구분도 여기서** — 재질만 바꾼다 |
| 리그 | 없음 |
| 애니메이션 | 오브젝트 트랜스폼만. 슬롯 대기 → 중앙 집어놓기 1종. **Blender에서 구워옴** |
| 포맷 | `.glb` (glTF 2.0). 스킨드 메시가 아니므로 Godot이 `MeshInstance3D` + `AnimationPlayer`로
  임포트 — `Skeleton3D`/`BoneAttachment3D` 불필요 |

**건틀릿이라서 유리한 점:**

- **금속이라 스포트라이트를 잡는다.** 어두운 씬에서 형태가 살아난다. 피부였으면
  같은 조명에서 밋밋했을 것이다
- **모델링 레퍼런스가 이미 세 장 있다** — `CardSprite.png`의 가위/바위/보 포즈

**표면 재질**은 형태와 별개로 조달한다 — [Metal Plate 02](https://polyhaven.com/a/metal_plate_02)
(Poly Haven, CC0). 원본은 녹슨 색이지만 **채도를 뺄 것이라 색은 상관없다** — 가져오는 건
Roughness/Normal에 있는 긁힘·마모 패턴이다. 2K로 받는다.

**Mixamo 오토리거는 안 맞는다** — 오토리거가 요구하는 게 "머리·몸통·팔·다리가 다 있는
이족보행 인간형"이라 팔뚝+손 조각을 올려서 새로 리깅을 맡기는 건 조건 자체를 못 채운다.

**다만 Mixamo의 완성된(이미 리깅된) 캐릭터를 베이스로 삼는 건 별개다** — 새로 리깅을 요청하는
게 아니라 이미 리깅된 걸 받아오는 것뿐이라 위 제약과 무관하다. 손가락 포즈를 카드 쥔 모양으로
맞춘 뒤 **Pose Mode → Armature 모디파이어 Apply**로 그 순간의 변형을 메시에 구워버리고,
Edit Mode에서 팔꿈치 위 버텍스를 지우고, 아마추어를 삭제하면 무리그 정적 메시가 남는다 —
지금 이 절이 요구하는 것과 정확히 같은 결과물이다. 라이선스는 CC0가 아니지만 완성된 게임에
포함시키는 용도로는 무료·무제한 사용이 허용된다(원본 파일 자체를 에셋팩처럼 재배포하는 것만
금지). CC0가 아니므로 이 파일을 받는 시점에 [ATTRIBUTIONS.md](ATTRIBUTIONS.md)에
"Mixamo 파생, 재배포 아님"으로 한 줄 기록해둘 것.

### 조달 후보

| 후보 | 라이선스 | 형태 | 비고 |
|---|---|---|---|
| **Fab** (fab.com, 구 Unreal Marketplace/Sketchfab Store) | Standard License — Unreal 외 툴에서도 사용 가능함이 명시돼 있음. 단, 옛 마켓 항목은 라이선스가 다를 수 있어 **항목별 확인 필요** | `.fbx`/`.glb` 직접 다운로드, Fab 런처의 엔진별 익스포트 파이프라인은 안 씀 | 품질 편차가 가장 적다. "gauntlet"/"glove"로 직접 검색해서 톤에 맞는 걸 고를 것 — 1순위 후보 |
| [PSX First Person Arms](https://drillimpact.itch.io/psx-first-person-arms-free) (Drillimpact) | **CC0** | `.glb`/`.fbx`/`.blend`, 리깅+18개 애니메이션(그중 "grabbing" 포함) | 팔뚝+손만 이미 분리돼 있어 몸통을 지울 필요가 없다. PS1풍 저해상도(512×512)라 그대로 안 쓰고 형태만 베이스로 씀 |
| [Quaternius Glove](https://poly.pizza/m/l1zv4LaA4I) | **CC0** | 정적 메시 | 손만 있는 훨씬 단순한 버전. 가벼운 형태 참고용 |
| Mixamo 캐릭터 | 무료(재배포만 금지) | 리깅된 전신, 손가락 관절 세밀 | 위 방법대로 자르면 형태 디테일이 가장 좋다 |

### 열린 갈림길 — 판금 건틀릿인가, 관절형 기계팔인가

**아직 정해지지 않았다.** [DESIGN.md](DESIGN.md)의 아트 방향은 판금 건틀릿(카드 속 손과
씬의 손이 같은 물건)을 전제로 쓰여 있는데, 관절로 이어진 기계팔이 기술적으로는 명백히 더 쌀
길이라는 게 나와서 여기 남겨둔다.

**기계팔이 유리한 이유:**

- **스키닝이 아예 필요 없다.** 기계팔은 부드럽게 휘지 않고 관절마다 뚝뚝 끊어져 회전하는 게
  정체성이라, `Skeleton3D`/웨이트 페인팅 대신 **`Node3D` 부모-자식 계층 자체가 리그다**
  (피벗 → 세그먼트 → 피벗 → 세그먼트…). 위에 적어둔 "판이 겹쳐 미끄러지게 리지드
  바인딩해야 한다"는 문제 자체가 없다 — 이음매가 보이는 게 결함이 아니라 디자인이다
- **Godot 에디터 안에서 바로 애니메이션할 수 있다.** `AnimationPlayer`로 각 피벗 노드의
  회전을 직접 키프레임 찍으면 되므로, 세그먼트 형태만 모델링해 오면(단순하면 Godot의
  `CylinderMesh`/`BoxMesh` 조합으로 대체 가능) **Blender에서 굽는 단계 자체를 스킵할 수
  있다.** 스킨 없는 노드 계층은 glTF로 가져와도 `Skeleton3D` 없이 순수 `Node3D` 트리 +
  `AnimationPlayer`로 깔끔하게 들어온다
- **조달이 더 쉬울 가능성이 높다** — "기계팔/로봇 암"은 SF·VR 에셋에서 흔해서 "gauntlet"보다
  CC0로 걸릴 확률이 높다

**다만 카드 아트와의 훅이 흔들린다** — 지금 카드 3장(가위/바위/보)에는 판금 건틀릿이 그려져
있고, 그게 씬의 손과 같은 물건이라는 게 아트 방향의 핵심 문장이다. 기계팔로 가면 그 세 장도
다시 그리거나("판금"의 연장선인 관절형 금속 손으로 재해석), 카드와 씬의 손이 다른 존재라는
불일치를 감수해야 한다.

**Godot 자체의 애니메이션 기능 관련** — Godot 에디터에는 `AnimationPlayer`라는 키프레임
타임라인 툴이 내장돼 있다. 이미 리깅된 스킨드 메시라면 본을 직접 포즈 잡고 그걸 키프레임
찍는 것도 Godot 안에서 된다. **Godot에 없는 건 "메시에 뼈를 심고 가중치를 칠하는 것"뿐**이고,
그건 여전히 Blender 영역이다 — 기계팔의 무리그 방식이 이 제약을 그냥 비켜간다.

### 카드

- **메쉬**: 얇은 상자, 12삼각형. **앞/뒤 UV 분리 필수** — 기본 `BoxMesh`는 앞뒤에 다른
  텍스처를 못 넣는다
- **두께**: 실측 0.3mm(`0.0003`)면 3D인지 2D인지 구분이 안 된다. **10~20배 과장할 것**
- 실제 자산은 메쉬가 아니라 **머티리얼** — 카드지 노멀맵, 가장자리, 반사
- **아트는 아홉 장 전부 필요하다.** 뒷면(`CardBackImage.png`, 1059×1485)만 그대로 쓴다.
  기존 앞면 세 장은 `CardSprite.png` 안에서 장당 약 90×128 — **뒷면보다 선형 12배 작아서**
  카메라 근처에서 뭉갠다. 여섯 장 신규 + 세 장 재작업

### 탁상

- 상판만. 다리/측면 불필요
- **머티리얼이 전부다.**
- **후보**: [Wood Table Worn](https://polyhaven.com/a/wood_table_worn) (Poly Haven, CC0) —
  이미 낡고 긁힌 목재라 톤이 맞는다. Diffuse/Normal/Roughness/AO 세트, **2K로 받는다**
  (해상도 근거는 아래 「임포트 설정」)
- **돌로 바꿀 필요 없다.** 모노크롬 규칙은 색상을 지우는 것이지 재질을 바꾸는 게 아니다 —
  **Diffuse(알베도)만 채도 0으로 저장**하고 Normal/Roughness는 그대로 둔다. 나뭇결·광택은
  거기 있어서 회색이 돼도 형태감은 그대로 산다. 채도 제거는 에디터에서 한 번 구워두는 쪽을
  권한다 — 런타임 셰이더로 매번 깎으면 에디터에서 보이는 색과 실제 실행 화면이 달라진다
- 크기가 조명에 구속된다 (위 참고)

### 저울 (승수 표시)

기둥 / 대들보 / 접시 2 / 토큰 1(인스턴싱). 단, 규칙과 어긋나지 않으려면:

> **10선승은 "차이"가 아니라 "먼저 10에 도달"이다.**

기울기를 주 정보로 만들면 7:6과 1:0이 비슷해 보이고 "몇 개 남았나"가 안 읽힌다.
**접시에 쌓인 토큰 개수가 주 정보, 기울기는 곁들이는 연출.** 접시마다 10칸 눈금.
움직임은 물리가 아니라 트윈 — 물리로 흔들면 토큰이 굴러떨어져서 개수가 안 읽힌다.

### 색 연출 — 조커만 채도를 가진다

전역 채도 제거를 걸 필요가 없다. **원본 텍스처를 처음부터 무채색으로 만들면**, 화면에서
채도를 가진 게 조커뿐이 된다 — 그러면 전역 채도 조절이 자동으로 조커 전용 다이얼이 된다:

```
Environment.AdjustmentEnabled = true
Environment.AdjustmentSaturation = 1.0 -> 2.5   // 조커 등장 시 트윈으로 올림
```

회색은 채도를 아무리 올려도 회색이라 나머지는 미동도 없고, **조커만 타오른다.** 색상 판별
셰이더나 마스크가 전혀 필요 없다 — 팔레트 규율(색이 한 곳에만 있음)이 선택성을 공짜로
만들어준 것.

글로우도 같은 원리다. 어두운 방에 스포트라이트 하나뿐이니, 조커 머티리얼에만 `Emission`을
주면 `Environment.GlowHdrThreshold`를 넘는 게 그것뿐이라 **조커만 빛난다.**

**주의: 키 라이트를 중성(약간 차갑게)으로 둘 것.** 벅샷 룰렛 특유의 누런 조명을 그대로
가져오면 화면 전체가 그 색에 물들어서, 조커가 화면에서 유일하게 채도를 가진 존재라는 전제가
깨진다. 분위기는 색이 아니라 **명암**(감쇠, 빛 웅덩이 크기, 볼류메트릭 포그)에서 뽑는다.

### 나중

- 소멸 파티클 (`GPUParticles3D`) — 메쉬 불필요. `CardDissolve.gdshader`를 `spatial`로 변환
- 조커 전용 연출 — 가장 강한 카드인데 지금은 다른 카드와 똑같이 사라진다
- 덱 더미 — 카드 메쉬 인스턴싱. 별도 자산 아님
- 타이머 — 제출 20초/선택 15초. 모래시계가 저울과 같은 계열이라 어울리지만,
  **남은 초를 정확히 못 읽는다.** 게이지는 2D `Control`로 남기고 모래시계는 장식으로

---

## 안개 — 검정에 삼켜지는 느낌

**`Camera3D`의 Far Clip은 이 용도가 아니다.** Far Clip은 그 거리를 넘으면 아예 안 그리는
하드 컷이라, 경계가 흐릿해지지 않고 뚝 끊겨서 사라진다. 성능용 백스톱으로 훨씬 멀리 잡아두는
용도로만 쓴다.

**`Environment`의 거리 안개가 원하는 것이다:**

```
Environment.FogEnabled = true
Environment.FogLightColor = Color.Black
Environment.FogDepthBegin = (선명하게 보이는 거리)
Environment.FogDepthEnd   = (완전히 검게 삼켜지는 거리)
```

이 구간 안에서 색이 서서히 검정과 섞인다. 구간을 좁게 잡으면 뚝 삼켜지는 느낌,
넓게 잡으면 서서히 스며드는 느낌 — 여기가 튜닝 포인트.

**균일하지 않고 뭉게뭉게 삼켜지길 원하면** `FogVolume` + `FogMaterial.DensityTexture`에
3D 노이즈를 물린다. 팔이 잘리는 지점 주변에 이 볼륨을 하나 놓으면 안개 밀도가 지점마다
들쭉날쭉해져서 진짜 구름처럼 보인다. 볼류메트릭 포그를 켜야 해서 비용이 더 들지만, 화면
대부분이 어차피 검고 오브젝트 수도 적어 부담은 크지 않을 것.

**권장 순서**: 거리 안개(위)로 기본 톤을 먼저 잡고, 부족하면 팔 절단부 근처에만 FogVolume을
겹친다. 광학적으로 흐려 보이게(초점 아웃까지) 하고 싶으면 `Camera3D`의 Depth of Field를
별도로 얹는다 — 안개의 색 혼합과는 다른 기능이라 겹쳐 써도 된다.

---

## 임포트 설정

해상도보다 이 설정들이 결과를 더 바꾼다. **Godot의 PNG 기본 프리셋은 2D용**이라 3D에
그대로 쓰면 안 된다.

- **Compress Mode -> VRAM Compressed.** 기본값(Lossless)은 3D에서 VRAM을 통째로 먹는다
- **Mipmaps -> 켤 것.** 2D 기본은 꺼져 있다. 비스듬히 놓인 카드·탁상은 밉맵 없으면 지글거린다
- **`TextureFilterEnum.LinearWithMipmapsAnisotropic`** — 스치는 각도로 보이는 면(특히 탁상)에
  필수. 이거 하나로 바닥이 뭉개지느냐 살아나느냐가 갈린다
- **노멀맵은 `NormalTexture`로 넣고 sRGB 끌 것.** 알베도와 같은 설정으로 넣으면 조명이
  이상해진다

### 해상도 기준 — 카드 크기로 계산됨

카드 높이가 88.9mm다. 텍스처를 거기 붙였을 때 실효 DPI:

| 해상도 | 실효 DPI |
|---|---|
| **1K** | **약 292** <- 실제 인쇄 카드 품질. 카드 아트는 이걸로 충분 |
| 2K | 585 |
| 4K | 1170 — 인쇄물보다 4배 정밀, 화면에서 안 보임 |

**4K는 과하다.** BC7 압축 기준 4K 한 장이 밉맵 포함 약 21MB, 카드 9종이면 190MB — 보이지
않는 디테일에 지불하는 비용이다. **카드는 1K, 탁상·팔 표면 텍스처는 2K(타일링이라 원본
크기보다 반복 횟수가 화면 밀도를 정한다).**

### glTF(`.glb`) 임포트 메모

- **좌표계가 Godot과 이미 같다.** glTF·Godot 둘 다 Y-up 오른손, 미터 단위. Blender는 Z-up이지만
  익스포터가 변환해줘서 **회전 보정이 필요 없다**
- **텍스처를 `.glb`에 임베드하지 말 것.** 임베드하면 임포트 시 Godot이 뜯어내면서 위의
  압축/밉맵 설정을 텍스처별로 못 만진다. 별도 PNG로 두고 참조하거나 임포트 후 추출
- **머티리얼은 Godot에서 다시 만드는 편이 낫다.** Blender 노드 머티리얼이 `StandardMaterial3D`로
  온전히 안 넘어오고, 스포트라이트에 금속이 어떻게 반응하는지가 이 씬의 진짜 자산이라 직접
  만져야 한다
- **스킨 없는(무리그) 애니메이션 노드 계층은 `Skeleton3D` 없이 순수 `Node3D` 트리 +
  `AnimationPlayer`로 들어온다** — 기계팔처럼 스켈레톤이 필요 없는 구조면 임포트가 더 깔끔하다

---

## 치수 — 1 unit = 1 미터

언리얼(1 uu = 1 cm)과 100배 차이.

| 대상 | 실측 | Godot |
|---|---|---|
| 카드 | 63.5 × 88.9 mm | `0.0635 × 0.0889` |
| 카드 두께 | 0.3 mm | `0.0003` → **과장 필요** |
| 17장 덱 | 약 5 mm | `0.005` |
| 탁상 높이 | 720 mm | `0.72` |
| 착석 시선 높이 | 약 1.2 m | 카메라 `Y ≈ 1.2` |

---

## 조달 — 정리

어둠 덕분에 외부 자산 수요 자체가 원래 예상보다 줄었다. 구체적인 후보는 흩어져 있으니
여기 모아만 둔다 — 각 항목의 근거는 해당 절 참고.

| 대상 | 후보 | 위치 |
|---|---|---|
| 탁상 | Wood Table Worn (Poly Haven, CC0) | 「탁상」절 |
| 팔 표면 재질 | Metal Plate 02 (Poly Haven, CC0) | 「팔」절 |
| 팔 형태 베이스 | Fab / PSX First Person Arms / Quaternius Glove / Mixamo | 「팔」절의「조달 후보」표 |

CC0를 우선하는 이유는 **크레딧 화면이 아직 없어서** CC BY가 부채로 남기 때문이다 — 단,
Mixamo처럼 CC0가 아니어도 재배포 금지 조건만 지키면 되는 출처는 예외로 쓴다(해당 절 참고).

[ATTRIBUTIONS.md](ATTRIBUTIONS.md)가 지금 **사운드만** 다룬다. 3D 자산을 처음 받는 시점에
같은 형식으로 섹션을 하나 더 만들 것.

---

## 순서

1. ~~아트 방향 적기~~ — [DESIGN.md](DESIGN.md) 「아트 방향」절로 완료
2. **카드 아트 9종** — 실질 블로커. 여섯 장 신규 + 세 장 재작업
3. **패를 어디에 두나 결정** (A: 탁상 / B: 부채꼴) — 팔 애니메이션 비용이 여기서 갈린다
4. **판금 건틀릿인가 기계팔인가 결정** — 팔 절의 열린 갈림길. 카드 아트(2번)와 맞물리므로
   되도록 2번보다 먼저, 최소한 카드 6종을 그리기 시작하기 전에 정할 것
5. **스파이크**: 카드 한 장 + 스포트라이트 + 검은 배경 + 거리 안개. 팔 없이.
   → 조명·안개가 이 게임의 아트라서, 팔보다 먼저 봐야 한다. 레시피는 「안개」·「색 연출」·
   「임포트 설정」 절에 이미 정리돼 있어 바로 적용 가능
6. 탁상 머티리얼 — [Wood Table Worn](https://polyhaven.com/a/wood_table_worn) 받아서
   알베도만 채도 제거
7. 팔 — 4번 결과에 따라 조달/모델링
8. 저울
