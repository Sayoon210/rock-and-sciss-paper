# 확장성 아이디어 낙서장

**이 문서는 확정된 설계가 아님.** 지금 만들 것도 아니고, 여기 적힌 대로 만들겠다는 약속도 아님.
"나중에 이런 걸 하고 싶어지면 어디부터 건드려야 하나"를 잊어버리기 전에 적어두는 메모.

- 확정된 게임 규칙 → [DESIGN.md](DESIGN.md)
- 확정된 기술 규칙 → [CLAUDE.md](CLAUDE.md)
- 실제로 내린 결정의 기록 → `DevLogDoc/`

여기 있는 항목이 확정되면 위 셋 중 하나로 옮기고 여기서는 지운다.
`?` 표시는 아직 답이 없는 질문.

---

## 1. 덱 장수 커스텀

지금은 **9장 고정** (`DeckAssembler`의 상수 `NORMAL_CARD_COPIES = 3` × 가위/바위/보).
공백·조커·능력카드는 전부 덱에서 빠졌다 — [DESIGN.md](DESIGN.md)의 「덱 구성」 참고.
밸런싱하려면 코드를 고쳐야 해서 불편함.

**하고 싶은 것**: 에디터에서 숫자만 바꿔서 덱 구성을 실험

**대략의 방향**
- `DeckRecipeData : Resource` 만들고 `[Export]`로 장수 노출, `.tres` 하나
- `DeckAssembler.BuildDeck(recipe)`가 받아서 조립
- 누구 레시피를 쓰나? → **호스트 것으로 양쪽 다 조립**이 제일 쉬움.
  클라가 보내는 값이 없으니 검증할 것도 없고, 코드도 거의 안 바뀜.
  클라에는 `MatchStartedRpc`에 숫자만 얹어서 표시용으로 전달.
- 플레이어별로 다른 덱은 아래 3번이랑 같은 문제 → 그때 같이

### ⚠ 이거 열기 전에 반드시 풀어야 할 것 — 덱 고갈

**지금 덱이 바닥나면 크래시남.** `Deck.TakeFromTop()`이 빈 리스트에서 `_cards[0]`을 집어서
라운드 해결 중간에 예외가 터짐 → 매치가 그대로 굳음.

지금 안 터지는 건 우연히 여유가 있어서지 설계된 게 아님:

| | 장수 |
|---|---|
| 시작 덱 | 20 |
| 멀리건 | −6 → 14 |
| 영구 소멸 가능 (공백4 + 조커2 + 능력5) | −11 |
| **최악의 경우 남는 덱** | **3** |

일반카드 9장은 덱 바닥으로 돌아가며 계속 순환하니까 소멸 카드 11장만 빠져나감.
여유 3장은 **설계가 아니라 우연**이고, 장수를 커스텀하게 만드는 순간 바로 깨진다.
(일반카드를 2장씩으로 줄이거나 공백를 늘리면 즉시 0 도달)

- ~~덱이 바닥나면 어떻게 되나~~ → **확정: 고갈된 쪽이 매치 패배.** [DESIGN.md](DESIGN.md)의 "덱 고갈"로 옮김.
  동시 고갈 / 효과 도중 고갈 두 가지도 코드로 확정함 (같은 문서 참고)
- ~~아직 코드에는 없음~~ → **구현됨.** `DeckAndHand.Draw()`가 빈 덱에서 예외 대신 `null`,
  `MatchSession.RecordResolvedRound`가 라운드마다 덱 카운트 0을 확인해 `Winner`에 반영
- 레시피를 실제로 커스텀하게 만들면 "이 레시피는 고갈 안 나나" 불변식 검증은 그때 별도로 필요
  (지금 구현은 "터지지 않는다"만 보장하지, "밸런스가 맞는 레시피인가"는 안 봄)

**순서**: ~~고갈 규칙 확정(DESIGN.md)~~ → ~~고갈 패배를 코드로 구현~~ → 레시피 Resource화 → 검증 → 클라 전달

---

## 2. 능력카드 선택 (덱빌딩)

`DESIGN.md`의 "확장성 메모"에 이미 언급된 것. **능력카드 풀이 지금의 4종보다 늘어나야 의미가 생김.**
지금은 4종이 전부라 "고를 것"이 없어서 만들어도 동작이 없음.

대화로 정해둔 전제 (확정 아님, 그때 다시 볼 것):
- 덱은 **매칭 버튼 누르기 전에** 미리 골라둠 → 그 다음 네트워크 연결 시작
- 중복 불가 — 종류당 1장씩만
- **덱 레시피가 정한 장수 정확히** 필수 (유동 아님)
- 상대 덱 구성은 매치 시작 시 **공개 안 함**
- 재대전 시 덱 유지 (매번 다시 안 고름)

**남는 질문**
- ? 5장 미만/초과로 매칭 버튼을 누르면 → 버튼 비활성? 경고?
- ? 상대가 뭘 골랐는지 매치 **끝난 뒤**엔 공개하나
- ? 풀이 늘면 일반/공백/조커 장수도 같이 조정해야 하나 (1번과 얽힘)

---

## 3. 클라이언트별 덱 저장 → 그 덱으로 매칭

2번의 자연스러운 다음 단계. 고른 덱이 게임 껐다 켜도 남아 있고, 그 덱을 들고 매칭에 들어감.

### 저장
- Godot의 `user://` 경로 (`user://deck.tres` 또는 JSON)
- 저장 시점: 덱 편집 완료 시 / 로드 시점: 게임 시작 시 1회
- 처음 켰거나 파일이 깨졌으면 → 기본 덱으로 폴백 (게임이 못 켜지면 안 됨)
- ? 덱 슬롯 여러 개 + 이름 붙이기까지 갈 것인가 (1개로 시작하는 게 맞을 듯)

### 매칭
```
[클라] 저장된 덱 로드 → 매칭 버튼 → 접속
                            │
                            ▼  덱 구성을 호스트로 전송
[호스트] 받은 덱을 재검증 ──→ 통과: MatchSession에 넣고 시작
                          └─→ 실패: 거부하고 이유 전달
```

### ⚠ 여기가 이 프로젝트에서 host-authority가 처음으로 진짜 문제가 되는 지점

지금까지는 덱을 **호스트가 양쪽 다 만들었기 때문에** 클라가 보내는 값이 아예 없었음.
클라별 덱이 생기는 순간 **클라이언트가 보낸 덱 구성을 호스트가 받게 됨.**

`user://` 파일은 유저가 직접 열어서 고칠 수 있음 → **조작된 덱이 그대로 올라올 수 있다고 가정해야 함.**
(조커 20장 덱, 존재하지 않는 카드 이름, 장수 안 맞는 덱 등)

호스트가 반드시 확인할 것:
- 총 장수가 규칙과 맞나
- 능력카드 장수가 레시피와 맞나, 중복 없나
- 실재하는 `ECardName`인가 (enum 범위 밖 정수가 올 수 있음)
- 일반/공백/조커 장수가 레시피와 맞나
- 고갈 불변식을 만족하나 (1번)

**이때 비로소 덱 구성 규칙을 `GameLogic`으로 옮기는 게 값을 함.**
- 검증은 Godot과 무관한 순수 규칙이고, 조합이 많아서 눈으로 확인하기 귀찮음 → 테스트로 덮을 가치가 생김
- `GameLogic/DeckComposition.cs`에 `Build(recipe, abilities)` + `Validate(deck)`
- `Scripts/Cards/DeckAssembler.cs`는 `CardDatabase`에서 카드 목록 긁어 넘기는 얇은 껍데기로 축소
- **지금 미리 옮기면 검증할 규칙이 없어서 껍데기만 생김** — 하지 말 것 (루트 CLAUDE.md의 "필요할까봐 미리 만들지 말 것")

### RPC로 덱을 어떻게 보내나
- `ECardName[]`을 그대로 못 보냄 → `CardChoiceCodec`처럼 `int[]`로 인코딩하는 방식 재사용
- 접속 직후 클라 → 호스트 단방향 1회. 호스트는 검증 후 자기 세션에만 반영, **상대에겐 안 알림** (비공개 원칙)

### 연결 흐름이 바뀜
지금은 접속되면 호스트가 바로 `HostStartsMatch()`를 누를 수 있음.
클라 덱을 기다려야 하니 **"클라 덱 도착"을 기다리는 단계가 하나 생김** = 사실상 로비.
- `EventBus`에 `ConnectedToServer` 시그널이 필요해짐 (현재 없음)
- 이 대기는 일회성/선형이라 `ToSignal()` await 방식이 어울릴 만한 자리

---

## 4. 재대전 흐름 (지금 비어 있음)

`GameState.ResetMatch()`는 있는데 **아무도 안 부름.**
매치가 끝나도 `MatchEnded` 시그널만 나가고 세션은 그대로 남음.

- 다시 `HostStartsMatch()`를 누르면 `_session`이 새 객체로 갈아끼워져서 굴러가긴 함
- 다만 `View`의 이전 점수/카드가 안 지워진 채 새 매치가 시작됨
- ? 재대전 합의를 받나 (양쪽 다 "한 판 더" 눌러야 시작) 아니면 호스트 단독인가
- 3번이 들어오면 "덱은 유지, 매치만 리셋"이 되어야 함 → `ResetMatch`와 `ResetConnection`의 경계가 그때 실제로 쓰임

---

## 5. 사운드 시스템

> **구현됨.** `AudioManager`, 버스, `ESoundName`, 설정 슬라이더가 전부 들어갔다.
> 아래에서 아직 유효한 것은 「히든 정보가 소리로 샐 수 있음」과 「카드에 AudioStreamPlayer를 붙이면 안 됨」
> 두 경고뿐이다. **무효**: 「어디에 사는가」·「소리를 트리거하는 주체」·「타이밍 상수」 —
> 세 절 전부 `MatchScreenUI`에 기대고 있는데 그건 `Deprecated/`로 갔다. 3D에서는
> `MatchWorldView`의 페이즈 머신이 그 자리고, 아직 소리가 물려 있지 않다.

지금 **소리가 하나도 없음.** 규모가 꽤 될 것 같아서 미리 구상만 해둠.

### 어디에 사는가

`GameLogic`은 소리를 못 냄 (Godot 참조 없음). 전부 `Scripts/` 일. 화면 셋(타이틀/접속/매치)이
같이 쓰니 Autoload가 맞고, 이름은 `AudioManager`
([Scripts/Autoload/CLAUDE.md](Scripts/Autoload/CLAUDE.md)의 `...Manager` = 수명/서비스).

**단, 이 Autoload의 일은 좁게**: 버스 관리, 스트림 로드/캐시, 동시 재생 처리, 볼륨.
**"언제 무슨 소리가 나는가"는 얘가 정하지 않음.** 아래가 그 이유.

### ⚠ 핵심 결정 — 소리를 트리거하는 주체

`AudioManager`가 소리를 **재생**하는 건 맞다. 문제는 **누가 "지금이다"라고 말하는가**다.

당연해 보이는 답은 "`AudioManager`가 `GameState` 시그널을 구독해서 알아서 튼다".
시그널이 연출보다 먼저 뜨는 것 자체는 **문제가 아님** — 딜레이 주고 예약하면 그만이다.

진짜 걸리는 건 **승패 연출 시점이 고정 딜레이로 계산이 안 된다**는 것:

```
OnOpponentCardFlipFinished ─┐
                            ├─ 둘 다 끝난 순간 → +0.35초 → 승패 연출
OnRoundResolved ────────────┘
```

승패 순간은 **독립적인 두 사건의 랑데부**다. 선택(교체/변화)이 없는 라운드면
`RoundResolved`가 공개와 같은 프레임에 와서 **뒤집기가 끝나기를 기다리고**, 선택이 있는
라운드면 뒤집기가 한참 전에 끝나 있고 **플레이어가 고를 때까지 기다린다.** 어느 쪽이
나중이냐가 매번 다르다.

`GameState` 시그널만 듣는 `AudioManager`는 **뒤집기가 언제 끝났는지 알 방법이 없다.**
그걸 알려면 `_opponentCardFlipPending` / `_roundAlreadyResolved` 인터록을 통째로 복제해야
하는데, **그건 이미 한 번 버그가 났던 그 로직이다**(조커 —
[devlog](DevLogDoc/2026-08-21-round-reveal-choreography.md)). 같은 상태 기계를 두 벌
두면 언젠가 미묘하게 갈라진다.

**그래서 결론은 "타이밍을 이미 쥐고 있는 쪽이 큐를 낸다"**:

| 소리 | 큐를 내는 곳 | 이유 |
|---|---|---|
| 뒤집기, 승/패, 소멸 시작, 필드 정리 | **`MatchScreenUI`** | 이미 그 시퀀스를 소유하고 상수도 갖고 있음. `OnPreFlipPauseTimeout` / `OnPostFlipPauseTimeout`이 **정확히** 그 순간임 |
| 멀리건/드로우 카드별 딜 | `HandView` | 카드마다 어긋내는 `ENTRY_STAGGER_SECONDS`를 얘만 앎 |
| 카드 집기/놓기, 버튼, 화면 전환 | 그 노드가 직접 | 애초에 시그널이 없음 |
| BGM | `AudioManager` | 화면 전환에 맞추면 되고 정밀할 필요 없음 |

즉 **연출 노드(`CardFlipEffect` 등)에 오디오를 물리지 않아도 된다** — 걔들은 시각만
담당하고, 큐는 이미 순서를 아는 `MatchScreenUI`에서 나간다. `AudioManager`는 끝까지
`Play(...)` 서비스로 남는다.

> 처음엔 이걸 "연출 노드가 직접 튼다"로 적었었는데 틀렸다. 근거로 든 "시그널이 먼저
> 뜬다"도 약했고(딜레이로 해결됨), 연출 노드까지 갈 필요도 없었다.

### ⚠ 히든 정보가 소리로 샐 수 있음

**카드별로 다른 소리를 내면, 상대가 뒷면으로 낸 카드가 뭔지 소리로 들통남.**

[2026-08-18](DevLogDoc/2026-08-18-first-real-screens.md)에서 "카드 종류 테두리를 항상
보이게"가 상대 패를 통째로 공개할 뻔했던 것과 **정확히 같은 실수의 소리 버전.**

- 상대 제출(`OpponentSubmitted`) 시점 소리는 **카드와 무관한 단일 소리**여야 함
- 카드별 소리는 **뒤집힌 뒤(`CardFlipEffect`가 앞면으로 바꾼 뒤)** 부터만 허용
- ? 소멸 소리를 조커/능력 구분해서 낼 것인가 → 이 시점엔 이미 공개됐으니 괜찮음

### ⚠ 카드에 AudioStreamPlayer를 붙이면 안 됨

`HandView`는 패가 갱신될 때마다 `CardView`를 **자유롭게 free함.** 카드의 자식으로
`AudioStreamPlayer`를 달면 **소리 나는 도중에 노드가 사라져서 뚝 끊김.**

`CardVanishEffect`가 "카드는 내 노드가 아니다, 건네받을 뿐"으로 설계된 것과 같은 이유.
소리는 카드보다 오래 사는 곳(= `AudioManager`)에서 재생하고, 카드는 요청만.

### 동시 재생

`AudioStreamPlayer` 하나는 스트림 하나만 재생함. 근데 이 게임은 **양쪽 카드가 동시에
소멸**하는 경우가 정상적으로 존재함 (조커는 항상 그럼).

- Godot 4의 `AudioStreamPolyphonic` + `AudioStreamPlaybackPolyphonic`이 이걸 위한 것 → 우선 검토
- 안 맞으면 `AudioStreamPlayer` 풀 (N개 돌려쓰기)
- ? 같은 소리가 같은 프레임에 두 번 나면 위상 겹쳐서 이상하게 커짐 — 살짝 어긋내거나 하나만 낼 것인가

### 데이터

루트 [CLAUDE.md](CLAUDE.md)가 데이터는 `Resource` + `.tres`라고 못박고 있고,
동시에 **"필요할까봐 미리 만들지 말 것"**도 못박고 있음. 그래서 단계적으로:

1. **최소**: `ESoundName` enum + `AudioManager`에 `[Export]`로 스트림 나열. 끝.
2. **반복이 귀에 거슬리기 시작하면**: `SoundData : Resource`로 승격.
   스트림 + `volume_db` + **피치 랜덤 범위**. 카드 내는 소리를 한 판에 50번 듣는데
   매번 완전히 동일하면 금방 피로해짐 — 피치를 ±5% 흔드는 것만으로 크게 나아짐.
3. 카드별 소리가 실제로 생기면 → `CardData`에 필드 추가할지, 별도 매핑을 둘지 그때 결정.
   (`CardData`는 이미 `DisplayName`/`Description`/`CardArt`를 들고 있으니 자연스럽긴 함)

`ESoundName`도 `ECardName`과 같은 함정 있음 — enum 순서가 `.tres`에 정수로 박히면
멤버를 중간에 끼워넣을 때 전부 밀림. 처음부터 알고 시작할 것.

### 버스 / 볼륨

- `Master` → `Music`, `SFX` 2개면 충분. `AudioServer.SetBusVolumeDb`
- ? 설정 화면이 아직 없음 — 볼륨 슬라이더를 어디 둘 것인가 (타이틀에 설정 버튼?)
- 저장은 `user://`. **3번(덱 저장)과 같은 문제이므로 같이 처리하는 게 이득** —
  "설정을 `user://`에 저장하고 시작할 때 읽는다" 껍데기를 두 번 만들 이유 없음
- 조작된 파일 걱정은 없음 (내 볼륨은 내 것). 3번과 달리 검증 불필요

### 소리 목록 초안

| 화면 | 순간 |
|---|---|
| 공통 | 버튼 호버 / 클릭, 화면 전환 |
| 매치 시작 | 셔플, 멀리건 딜(카드마다 stagger — 이미 `ENTRY_STAGGER_SECONDS` 있음) |
| 제출 | 카드 집기, 제출칸에 놓기, **상대 제출**(카드 무관 단일음) |
| 공개 | 카드 뒤집기 |
| 승패 | 승 스팅 / 패 스팅 / 무승부 |
| 카드 처리 | 덱 바닥으로 복귀, 소멸(부서지는 소리 — `CardVanishEffect` 진행에 맞춰) |
| 능력 | 리셋(패 전체 회수 + 재배분), 교체, 변화, 드로우 |
| 매치 종료 | 승리 / 패배 |
| BGM | 타이틀, 매치 중 |

### 파일

- `Assets/Audio/` 신설. SFX는 `.wav`(지연 없음), BGM은 `.ogg`(스트리밍)
- 임포트 설정에서 SFX 루프 끄기 — 켜져 있으면 한 번 나고 안 멈춤

### ⚠ 소리가 들어오면 타이밍 상수를 다시 봐야 함

`MatchScreenUI`의 `PRE_FLIP_PAUSE_SECONDS`(0.25) / `POST_FLIP_PAUSE_SECONDS`(0.35) /
`FIELD_CLEAR_DELAY_SECONDS`(1.5)는 **눈으로만 맞춘 값.** 소리는 타이밍 오차를 훨씬
잔인하게 드러냄 — 스팅이 연출보다 늦으면 즉시 티가 남. 소리를 붙인 뒤 전부 재조정 각오.

**순서**: `AudioManager` + 버스 + 최소 enum → UI 소리 하나로 배선 검증 → 연출 노드에
하나씩 → 반복 피로 느껴지면 `SoundData` 승격 → 설정/저장(3번과 함께)

---

## 6. 3D로 다시 만들기

> **했다.** `rebuild-3d` 브랜치가 이것이다. 아래는 착수 전에 적은 계획이므로
> **지시문이 아니라 기록으로 읽을 것.** 특히 「지금 할 일 — 없음. 아직 하지 말 것」은
> 이미 뒤집혔다. 예측이 어떻게 틀렸는지는
> [DevLogDoc/2026-08-28-presentation-layer-swap.md](DevLogDoc/2026-08-28-presentation-layer-swap.md)와
> [DevLogDoc/2026-08-29-3d-presentation-pitfalls.md](DevLogDoc/2026-08-29-3d-presentation-pitfalls.md)에 있다.
>
> 아직 안 끝난 것만 옮겨 적으면: 연출 4종(뒤집기·승패·소멸·필드정리) 중 3D에서 다시
> 만들어진 것은 뒤집기뿐이고, 툴팁은 대체물 없이 그냥 사라졌다.

게임이 단순해서 연출로 먹고 들어가는 편이 낫다는 판단. 카드가 실제로 뒤집히고, 빛나고,
부서지는 걸 2D에서 흉내내는 것보다 3D에서 그냥 하는 게 낫다는 쪽.

### 결론부터 — "다시 만들기"가 실제로는 `Scripts/UI/`만 다시 만드는 것

이 프로젝트에서 **어려운 부분은 전부 3D와 무관한 곳에 있다.** 그래서 "포팅이냐 재작성이냐"의
차이가 생각보다 작다. 새 프로젝트로 시작하더라도 아래 둘은 **그대로 복사해 온다:**

| | 작업량 | 이유 |
|---|---|---|
| `GameLogic/` 전부 | **0** | Godot 참조가 없어서 2D/3D 개념 자체가 없음. 룰·판정·덱·효과 그대로 |
| `Scripts/Autoload/` 거의 전부 | **거의 0** | `GameState`는 `MatchView`와 RPC만 다루지 화면을 모름. 네트워크·카드DB·오디오 무관 |
| `Data/Cards/*.tres` | **0** | `Texture2D`는 3D에서 머티리얼 알베도로 그대로 씀 |
| `Scripts/UI/` 약 3,800줄 | **여기가 전부** | 아래 |

**검증 기준: 새 프로젝트에서 `GameLogic` 테스트 134개가 손 안 대고 그대로 통과해야 한다.**
안 되면 뭔가 표현 계층으로 샌 것이고, 그건 지금 구조가 무너졌다는 뜻이다.

즉 재작성을 하더라도 **호스트 권위·히든 정보·라운드 판정 우선순위를 다시 짜는 일은 없다.**
그 셋이 이 게임에서 실제로 어려웠던 부분이다.

### 다시 써야 하는 것

`Control` 기반 8개 + `Node2D` 기반 2개. 무게순:

| 파일 | 난이도 | 비고 |
|---|---|---|
| `HandView` (756줄) | **상** | 손패 배치·슬롯 안정성이 전부 2D 좌표 계산. 3D에선 수학이 통째로 다름 |
| `CardView` (454줄) | **상** | 드래그·히트테스트·툴팁이 다 여기 |
| `MatchScreenUI` (1063줄) | **중** | 대부분 시퀀싱/상태 로직이라 살아남음. 노드 참조와 좌표만 교체 |
| 연출 4종 | **하~중** | 아래 |
| `DeckView` (191줄) | **중** | |
| `MatchDebugUI`, 타이틀/접속 화면 | **0** | 2D UI로 그냥 남김 |

### 걸리는 지점 4개

**1. 드래그와 히트테스트가 2D 좌표에 직접 붙어 있음 — 제일 큰 덩어리.**
그나마 Godot의 `_GetDragData`/`_DropData` 시스템은 안 쓰고 직접 짜여 있어서 낫다. 하지만
[CardView.cs:281](Deprecated/Scripts/UI/CardView.cs:281)이 `GlobalPosition = GetGlobalMousePosition() - _dragGrabOffset`,
[CardDropZone.cs:45](Deprecated/Scripts/UI/CardDropZone.cs:45)가 `GetGlobalRect().HasPoint(...)`다.
3D에선 **카메라 레이캐스트 + 평면 교차**로 전부 새로 짠다.

**2. 툴팁은 못 가져간다.** `_MakeCustomTooltip`/`_GetTooltip`은 `Control` 전용이고 3D에
대응물이 없다. 등장 시점·위치·해제를 Godot이 다 해주던 걸([Scripts/CLAUDE.md](Scripts/CLAUDE.md)가
"직접 만들지 말라"고 못박은 바로 그것) 이번엔 직접 만들어야 한다. **재작성을 한다면 여기가
새로 설계할 지점이지 옮겨올 지점이 아니다.**

**3. 연출은 오히려 나아지는 게 있다.**
- `CardFlipEffect` — 지금은 `Scale.X`를 찌그러뜨려 뒤집기를 **흉내내는** 중. 3D에선 그냥
  Y축 회전이라 더 간단하고 더 잘 나온다. **순이득.**
- `CardOutcomeEffect` — `Panel` + `StyleBoxFlat` 그림자로 만든 후광. 3D엔 이 기법이 없어서
  이미시브 머티리얼 + 글로우 버스로 다시 만든다. 결과는 더 자연스러울 것.
- `CardVanishEffect` — `Shaders/CardDissolve.gdshader`가 `shader_type canvas_item`.
  `spatial`로 다시 쓰되 디졸브 로직 자체는 거의 그대로 옮겨진다.

**4. 사운드는 무관하다.** 5번의 "타이밍을 쥔 쪽이 큐를 낸다"가 `MatchScreenUI`에 붙어 있어서,
그 호출 위치만 살아 있으면 된다. 오히려 `AudioStreamPlayer3D`로 위치감을 줄 여지가 생긴다.

### 전면 3D 말고 하이브리드

Godot은 3D 뷰포트 위에 `Control`을 그대로 얹을 수 있다.

- **카드 필드만 3D** — 카드, 덱, 손패, 연출
- **점수판·프롬프트·타이머·매치종료 오버레이는 `Control` 그대로**

이러면 `MatchDebugUI`·타이틀/접속 화면·프롬프트 스트립 관련 코드가 전부 살아남아서
재작성 범위가 체감상 절반이 된다. `?` 손패를 3D로 하면 드래그가 3D인데 드롭존이 2D UI인
경계가 생기는데, 이게 오히려 헷갈릴지는 해봐야 안다.

### 지금 할 일 — 없음. 대신 지켜둘 것 하나

**아직 하지 말 것.** UI가 계속 바뀌는 중이라(승패 연출만 두 번 갈아엎었고 사운드도 방금 붙음)
지금 옮기면 그 반복 비용이 두 배가 된다.

대신 **지금 공짜로 지켜둘 수 있는 것**: `MatchScreenUI`에서 *시퀀싱 로직*과 *좌표/노드 조작*이
섞이지 않게 유지하기. 지금은 잘 나뉘어 있다 — `OnPreFlipPauseTimeout`은 타이밍만 정하고 실제
움직임은 연출 노드가 갖는다. **이 경계가 유지되는 한 3D 전환은 연출 노드 교체로 끝난다.**
반대로 좌표 계산이 `MatchScreenUI`로 새어들어가기 시작하면 그때부터 비싸진다.

### 재작성한다면 그대로 가져갈 관례

새로 만든다고 처음부터 다시 정할 필요 없는 것들:

- 연출 노드 패턴 — `Play(...)`/`Stop()`/`IsPlaying` + 수동 `SetProcess` + `_Process(delta)`.
  `Tween`도 `async`도 안 씀
- "카드는 내 노드가 아니다, 건네받을 뿐" — `CardVanishEffect`가 그렇게 설계된 이유
  (`HandView`가 `CardView`를 자유롭게 free함)
- 5번의 사운드 큐 소유 규칙
- `E` 접두사 enum, `SCREAMING_SNAKE_CASE` const 등 [CLAUDE.md](CLAUDE.md) 명명 규칙 전부
- 히든 정보 규율 — 3D가 되어도 상대 패는 뒷면이다

---

## 7. 기타 잡생각

- `DeckAssembler`는 `CardDatabase`가 `null`일 때만 예외를 던짐.
  `.tres` 하나가 빠져서 능력카드가 4종만 로드되면 **조용히 19장 덱을 만듦.**
  지금은 10개 다 있어서 안 나는 상황. `.tres`를 손댈 일 생기면 그때 가드 생각하기.
- 능력카드가 늘어나면 `RoundResolver`의 우선순위 규칙(리셋 최우선 → 나머지 P1→P2)도
  같이 복잡해질 것. 지금은 리셋 하나만 예외 처리라 버티는 중.
- **예지 카드가 다시 가능해졌다.** 원래 "낸 뒤에 보고 나서 고르는 카드는 없다"는
  `DESIGN.md` 보장과 충돌해서 삭제됐는데, 교체/변화가 공개 후 선택으로 바뀌면서
  그 보장이 사라졌고 라운드 중간 왕복도 이미 존재한다. 되살릴지는 별개 문제 —
  선택 대기가 늘어날수록 라운드가 느려지는 건 감안해야 함.
